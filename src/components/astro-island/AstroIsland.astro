---
import { BodyLong } from '@navikt/ds-react';
import { exampleApiUrl } from '@src/utils/server/urls';

const exampleData: any = await fetch(exampleApiUrl)
  .then((response) => response.json())
  .then((data) => data);
---

<BodyLong>
  Navn: {exampleData.name}
</BodyLong>
